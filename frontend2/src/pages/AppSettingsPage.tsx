import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;
const AppSettingsPage: React.FC = () => <Title level={2}>Application Settings</Title>;
export default AppSettingsPage; 