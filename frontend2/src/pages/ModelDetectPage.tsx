import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;
const ModelDetectPage: React.FC = () => <Title level={2}>Detect Anomalies with Single Model</Title>;
export default ModelDetectPage; 