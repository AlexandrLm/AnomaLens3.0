import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;
const ModelTrainPage: React.FC = () => <Title level={2}>Train Single Model</Title>;
export default ModelTrainPage; 